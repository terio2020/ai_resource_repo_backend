<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.repo.mapper.MemoryMapper">

    <resultMap id="BaseResultMap" type="com.ai.repo.entity.Memory">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="uid" property="uid" jdbcType="VARCHAR"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="agent_id" property="agentId" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="version" property="version" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="LONGVARCHAR"/>
        <result column="file_path" property="filePath" jdbcType="VARCHAR"/>
        <result column="file_size" property="fileSize" jdbcType="BIGINT"/>
        <result column="mime_type" property="mimeType" jdbcType="VARCHAR"/>
        <result column="tags" property="tags" jdbcType="VARCHAR"/>
        <result column="category" property="category" jdbcType="VARCHAR"/>
        <result column="is_public" property="isPublic" jdbcType="BOOLEAN"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="download_count" property="downloadCount" jdbcType="INTEGER"/>
        <result column="like_count" property="likeCount" jdbcType="INTEGER"/>
        <result column="metadata" property="metadata" jdbcType="LONGVARCHAR"/>
        <result column="memory_type" property="memoryType" jdbcType="VARCHAR"/>
        <result column="sharing_scope" property="sharingScope" jdbcType="VARCHAR"/>
        <result column="owner_type" property="ownerType" jdbcType="VARCHAR"/>
        <result column="client_memory_key" property="clientMemoryKey" jdbcType="VARCHAR"/>
        <result column="schema_version" property="schemaVersion" jdbcType="VARCHAR"/>
        <result column="revision" property="revision" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        uid, id, user_id, agent_id, title, content, version, description, file_path, file_size,
        mime_type, tags, category, is_public, status, download_count, like_count, metadata,
        memory_type, sharing_scope, owner_type, client_memory_key, schema_version, revision,
        created_at, updated_at
    </sql>
    <insert id="insert" parameterType="com.ai.repo.entity.Memory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO memories (uid, user_id, agent_id, title, content, version, description, file_path, file_size,
                           mime_type, tags, category, is_public, download_count, like_count, metadata,
                           memory_type, sharing_scope, owner_type, client_memory_key, schema_version, revision)
        VALUES (#{uid}, #{userId}, #{agentId}, #{title}, #{content}, #{version}, #{description}, #{filePath}, #{fileSize},
                #{mimeType}, #{tags}, #{category}, #{isPublic}, #{downloadCount}, #{likeCount}, #{metadata},
                #{memoryType}, #{sharingScope}, #{ownerType}, #{clientMemoryKey}, #{schemaVersion}, #{revision})
    </insert>

    <update id="update" parameterType="com.ai.repo.entity.Memory">
        UPDATE memories
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="agentId != null">agent_id = #{agentId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="version != null">version = #{version},</if>
            <if test="description != null">description = #{description},</if>
            <if test="filePath != null">file_path = #{filePath},</if>
            <if test="fileSize != null">file_size = #{fileSize},</if>
            <if test="mimeType != null">mime_type = #{mimeType},</if>
            <if test="tags != null">tags = #{tags},</if>
            <if test="category != null">category = #{category},</if>
            <if test="isPublic != null">is_public = #{isPublic},</if>
            <if test="downloadCount != null">download_count = #{downloadCount},</if>
            <if test="likeCount != null">like_count = #{likeCount},</if>
            <if test="metadata != null">metadata = #{metadata},</if>
            <if test="memoryType != null">memory_type = #{memoryType},</if>
            <if test="sharingScope != null">sharing_scope = #{sharingScope},</if>
            <if test="ownerType != null">owner_type = #{ownerType},</if>
            <if test="clientMemoryKey != null">client_memory_key = #{clientMemoryKey},</if>
            <if test="schemaVersion != null">schema_version = #{schemaVersion},</if>
            <if test="revision != null">revision = #{revision},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM memories WHERE id = #{id}
    </delete>

    <delete id="deleteByAgentId" parameterType="java.lang.Long">
        DELETE FROM memories WHERE agent_id = #{agentId}
    </delete>

    <delete id="deleteGeneralByAgentId">
        DELETE FROM memories WHERE agent_id = #{agentId} AND memory_type = 'GENERAL'
    </delete>

    <update id="detachProfileByAgentId">
        UPDATE memories SET agent_id = NULL
        WHERE agent_id = #{agentId} AND memory_type = 'USER_PROFILE'
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        ORDER BY created_at DESC
    </select>

    <select id="adminSelectPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        <where>
            <if test="keyword != null and keyword != ''">
                (title LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\'
                 OR description LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\')
            </if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="adminCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM memories
        <where>
            <if test="keyword != null and keyword != ''">
                (title LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\'
                 OR description LIKE CONCAT('%', #{keyword}, '%') ESCAPE '\\')
            </if>
            <if test="status != null and status != ''">AND status = #{status}</if>
        </where>
    </select>

    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserIdVisibleToAgent" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId} AND status = 'VISIBLE'
          AND (
            agent_id = #{agentId}
            OR (memory_type = 'USER_PROFILE' AND sharing_scope = 'USER_AGENTS')
            OR (memory_type = 'GENERAL' AND is_public = TRUE)
          )
        ORDER BY created_at DESC
    </select>

    <select id="selectByAgentId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE agent_id = #{agentId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByCategory" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE category = #{category} AND status = 'VISIBLE'
        ORDER BY created_at DESC
    </select>

    <select id="selectByCategoryVisibleToUser" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE category = #{category} AND status = 'VISIBLE'
          AND (
            (is_public = TRUE AND memory_type = 'GENERAL')
            OR (#{agentId} IS NULL AND user_id = #{userId})
            OR (agent_id = #{agentId})
            OR (user_id = #{userId} AND memory_type = 'USER_PROFILE' AND sharing_scope = 'USER_AGENTS')
          )
        ORDER BY created_at DESC
    </select>

    <select id="selectByPublic" parameterType="java.lang.Boolean" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE is_public = #{isPublic} AND status = 'VISIBLE'
        ORDER BY created_at DESC
    </select>

    <select id="searchByKeyword" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE MATCH(title, content, tags) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE)
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserIdAndPublic" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId} AND is_public = #{isPublic} AND status = 'VISIBLE'
        ORDER BY created_at DESC
    </select>

    <select id="selectByAgentIdAndPublic" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE agent_id = #{agentId} AND is_public = #{isPublic} AND status = 'VISIBLE'
        ORDER BY created_at DESC
    </select>

    <select id="searchPublicByKeyword" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE is_public = TRUE
        AND status = 'VISIBLE'
        AND MATCH(title, content, tags) AGAINST(#{keyword} IN NATURAL LANGUAGE MODE)
        ORDER BY created_at DESC
    </select>

    <delete id="batchDelete">
        DELETE FROM memories WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="batchDeleteOwned">
        DELETE FROM memories
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">#{id}</foreach>
          AND user_id = #{userId}
          <if test="agentId != null">AND agent_id = #{agentId}</if>
    </delete>

    <select id="selectByUserIdAndAgentIdAndTitle" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId} AND agent_id = #{agentId} AND title = #{title}
    </select>

    <select id="selectByUserIdAndAgentIdAndClientKey" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId} AND agent_id = #{agentId} AND client_memory_key = #{clientMemoryKey}
    </select>

    <select id="selectProfileByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE user_id = #{userId}
          AND memory_type = 'USER_PROFILE'
          AND sharing_scope = 'USER_AGENTS'
          AND status = 'VISIBLE'
        ORDER BY updated_at DESC
    </select>

    <update id="updateByCompositeKey" parameterType="com.ai.repo.entity.Memory">
        UPDATE memories
        SET content = #{content},
            version = #{version},
            description = #{description},
            file_path = #{filePath},
            file_size = #{fileSize},
            mime_type = #{mimeType},
            tags = #{tags},
            category = #{category},
            is_public = #{isPublic},
            download_count = #{downloadCount},
            like_count = #{likeCount},
            metadata = #{metadata}
            , memory_type = #{memoryType}
            , sharing_scope = #{sharingScope}
            , owner_type = #{ownerType}
            , client_memory_key = #{clientMemoryKey}
            , schema_version = #{schemaVersion}
            , revision = #{revision}
        WHERE user_id = #{userId} AND agent_id = #{agentId} AND title = #{title}
    </update>

    <update id="incrementDownloadCount">
        UPDATE memories
        SET download_count = download_count + 1
        WHERE id = #{id}
    </update>

    <update id="incrementLikeCount">
        UPDATE memories
        SET like_count = like_count + 1
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE memories
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <select id="selectCountByAgentIds" resultType="com.ai.repo.dto.AgentIdCount">
        SELECT agent_id, COUNT(*) AS count
        FROM memories
        WHERE agent_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        GROUP BY agent_id
    </select>

    <select id="selectByUid" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM memories
        WHERE uid = #{uid}
    </select>

</mapper>
